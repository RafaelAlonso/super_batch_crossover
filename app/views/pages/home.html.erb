<div class="container">
  <% if user_signed_in? %>
    <h1>Welcome <%= current_user.first_name %> <%= current_user.last_name %></h1>
  <% else %>
    <h1>HOLD! Identify yourself</h1>
  <% end %>

  <p>Find me in app/views/pages/home.html.erb</p>
</div>